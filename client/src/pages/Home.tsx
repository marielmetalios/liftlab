import LoginContainer from "../components/login";
import QuoteContainer from "../components/quoteContainer";

const Home = () => {
    return (
      <div>
        <LoginContainer />
        <QuoteContainer />
      </div>
    );
  }

  export default Home;